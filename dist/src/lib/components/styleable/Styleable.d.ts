declare abstract class Styleable extends HTMLElement {
    private state;
    private setState;
    private getPosition;
    private getStyle;
    connectedCallback(): void;
}
export default Styleable;
